import { BgColorStyleDirective } from './bg-color-style.directive';

describe('BgColorStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new BgColorStyleDirective();
    expect(directive).toBeTruthy();
  });
});
